# Cursor adapter

Install the canonical `skills/adhd-mode/` directory in `.cursor/skills/adhd-mode/` or the user-level skills directory supported by your Cursor version.

Use a real directory copy rather than a symlink so Windows clones and ZIP downloads remain reliable. Do not edit this adapter as a second policy source.
