# ADHDMode

When ADHDMode is active, read and apply `skills/adhd-mode/SKILL.md`.

The user may activate it by saying `enable ADHDMode` and stop it with `disable ADHDMode`, `stop ADHDMode`, or `normal mode`.
